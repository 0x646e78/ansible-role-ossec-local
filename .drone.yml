pipeline:
steps:
- name: build
  image: alpine
  environment:
    USERNAME:
      from_secret: username
  commands:
  - echo ${USERNAME}

secrets:
  docker_username:
    external:
      name: secret/docker#username

  docker_password:
    external:
      name: secret/docker#password
